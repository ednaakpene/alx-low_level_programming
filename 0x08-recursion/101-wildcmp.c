#include "main.h"

/**
  * wildcmp - compares two strings
  * @s1: first parameter of function
  * @s2: second parameter of function
  * Return: return 1 if strings are identical, 0 if not
  */

int wildcmp(char *s1, char *s2)
}

